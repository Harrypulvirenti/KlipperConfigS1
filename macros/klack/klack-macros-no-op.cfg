[gcode_macro Home_Lock]
gcode:
    {action_respond_info("You are using a BLTouch instead of Klack. This macro should not be used")}
    G28

[gcode_macro Attach_Probe]
gcode:
    {action_respond_info("You are using a BLTouch instead of Klack. This macro should not be used")}

[gcode_macro Dock_Probe]
gcode:
    {action_respond_info("You are using a BLTouch instead of Klack. This macro should not be used")}

[gcode_macro Dock_Probe_Unlock]
gcode:
    {action_respond_info("You are using a BLTouch instead of Klack. This macro should not be used")}
